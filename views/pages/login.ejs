<html>

<head>
  <link href="/stylesheets/style.css" rel="stylesheet" type="text/css">
  <% include("../functions") %> 
</head>

<body>
  <div class="container" id="login-container">
    <div class="wrapper">
      <span class="eyebrow">Square Developer</span>
      <h1>Gift Card Sample App</h1>
      <span>You can easily manage your gift cards with Square Gift Cards. This sample app 
        demonstrates how Square Gift Cards API may be used by a customer to manage their gift cards.
      </span>
      <h5>Login</h5>
      <div class="pretty-dropdown" id="login-dropdown">
        <div id="login-dropdown__selected-content" class="pretty-dropdown__selected" onclick="showOptions()">
          <div class="pretty-dropdown__picture">
            <img src="/images/profile.svg"/>
          </div>
          <div class="pretty-dropdown__selected-info">
            <span id="pretty-dropdown__selected-primary">Customer</span>
            <span id="pretty-dropdown__selected-secondary"><%= customers[0].givenName %> <%= customers[0].familyName %></span>
          </div>
          <span class="pretty-dropdown__icon">
            <img src="/images/caret.svg" alt=""/>
          </span>
        </div>
        <ul id="login-dropdown__options" class="pretty-dropdown__options">
        <% customers.forEach(function(customer) { %>
          <li onclick="select(this)" pretty-dropdown-primary-value="Customer" pretty-dropdown-value="<%= customer.id %>" pretty-dropdown-secondary-value="<%= customer.givenName %> <%= customer.familyName %>">
            <div class="pretty-dropdown__option"><%= customer.givenName %> <%= customer.familyName %></div>
          </li>
        <% }); %>
        </ul>
        <form action="/login" method="POST">
          <input name="customer" id="pretty-dropdown__value" hidden=true type="text" value="<%= customers[0].id %>" />
          <button type="submit">Login</button>
        </form>
        <span class="message">
          Don’t forget you can manage Customers and Gift Cards using Square’s API Explorer to call the API. This sample app will update accordingly.
        </span>
      </div>
      
    </div>
  </div>
  <script src="/js/pretty-dropdown.js"></script>
  <script>
    initializeDropdown("login-dropdown");
  </script>
</body>

</html>