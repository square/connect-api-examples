{
  "name": "order-ahead-sample-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=production nodemon -e js,pug ./bin/www",
    "test": "NODE_ENV=sandbox nodemon -e js,pug ./bin/www",
    "seed": "NODE_ENV=sandbox node ./bin/script/seed-catalog.js generate",
    "clear": "NODE_ENV=sandbox node ./bin/script/seed-catalog.js clear",
    "help": "NODE_ENV=sandbox node ./bin/script/seed-catalog.js --help"
  },
  "dependencies": {
    "body-parser": "*",
    "cookie-parser": "*",
    "debug": "*",
    "eslint": "^6.1.0",
    "express": "*",
    "morgan": "*",
    "pug": "*",
    "request": "^2.88.0",
    "serve-favicon": "*",
    "square-connect": "^3.20200528.0"
  },
  "devDependencies": {
    "nodemon": "^1.19.0"
  }
}
